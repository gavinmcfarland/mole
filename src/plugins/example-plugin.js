import _ from "lodash";
import v from 'voca';

export default function({config, output, prop: margin}) {
	let abbriations = { 'm': [ 't', 'r', 'b', 'l']}
	// @param config
	// @param output
	// @param prop
}
